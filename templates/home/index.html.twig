{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
<h1>Test</h1>
	{% for task in tasks %}
		<ul>
			<li>{{ task.title }}</li>
			<li><a href="{{ path('app_task_detail', {id: task.id}) }}">Détail</a></li>
		</ul>
	{% endfor %}
{% endblock %}

